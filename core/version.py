"""
core/version.py — NgpCraft Engine application version.

Single source of truth for the version string used in the About dialog,
window title, and auto-update check.
"""

from __future__ import annotations

APP_VERSION = "1.0.0"

# GitHub repository used for release checks (owner/repo).
APP_GITHUB_REPO = "Tixul/Ngpcraft_Engine"
